#include <stdio.h>
#include <stdlib.h>

int main(){
  
    // arithmetic operators
 
    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement
 
    int x = 5;
    int y = 2;
    
    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    float z = x / (float) y; // Explicite type casting.
    // int z = x % y;
    // x++;
    // y--;

    printf("%.1f\n", z);
    // printf("%d", y);
    
    system("pause");
    return 0;
}